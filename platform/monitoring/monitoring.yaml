project: default
source:
  repoURL: 'https://github.com/sandersch/homelab.git'
  path: platform/monitoring
  directory:
    recurse: false
    jsonnet:
      libs:
      - platform/monitoring/vendor
destination:
  namespace: monitoring
  name: in-cluster
syncPolicy:
  automated:
    prune: true
    selfHeal: true
  syncOptions:
    - CreateNamespace=true

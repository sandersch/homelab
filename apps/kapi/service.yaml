apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: kapi
    process: web
  name: kapi-web
  namespace: default
spec:
  ports:
  - name: http
    port: 3000
    protocol: TCP
    targetPort: http
  selector:
    app.kubernetes.io/name: kapi
    process: web
  type: ClusterIP

kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: elasticsearch-master-elasticsearch-master-1
spec:
  storageClassName: nvme0
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
